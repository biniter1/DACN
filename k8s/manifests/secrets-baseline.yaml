# ============================================================
# secrets-baseline.yaml — Secret lưu plain text trong K8s
#
# BASELINE: Secret được lưu dưới dạng K8s Secret (base64 only)
# Không có encryption at rest riêng, không có Secrets Manager
# Bất kỳ ai có quyền read namespace đều decode được
#
# Demo KH4: kubectl get secret → base64 decode → thấy plain text
# (Zero Trust: ESO lấy từ AWS Secrets Manager, Pod Identity scoped)
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: boutique-secrets
  namespace: boutique-baseline
type: Opaque
stringData:
  # BASELINE: Secret lưu thẳng trong K8s Secret
  # Bất kỳ SA nào trong namespace đều có thể đọc nếu không restrict RBAC
  REDIS_PASSWORD: "r3d1s-p@ssw0rd-2024"
  PAYMENT_API_KEY: "pk_live_demo_51NxXxXxXxXxXxXxXxXxXxXx"
  INTERNAL_API_TOKEN: "Bearer eyJhbGciOiJIUzI1NiJ9.demo"
